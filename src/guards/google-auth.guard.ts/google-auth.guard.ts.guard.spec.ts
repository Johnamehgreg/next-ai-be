import { GoogleAuthGuardTsGuard } from './google-auth.guard.ts.guard';

describe('GoogleAuthGuardTsGuard', () => {
  it('should be defined', () => {
    expect(new GoogleAuthGuardTsGuard()).toBeDefined();
  });
});
